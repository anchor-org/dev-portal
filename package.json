{
  "name": "dev-portal",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "redocly build-docs docs/openapi.yaml -o index.html --title \"Anchor API\" && node scripts/inject-favicon.mjs",
    "test": "vitest run"
  },
  "devDependencies": {
    "@redocly/cli": "^1.34.3",
    "@types/node": "^22.13.0",
    "typescript": "^5.7.3",
    "vitest": "^2.1.9"
  }
}
